{
  "name": "mcp-guard",
  "version": "0.1.1",
  "description": "Security analysis MCP server — scan for secrets, check CVEs, audit HTTP headers",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-guard": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "mcp",
    "security",
    "secrets",
    "cve",
    "claude",
    "cursor"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
